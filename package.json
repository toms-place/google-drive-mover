{
  "name": "google-drive-mover",
  "version": "0.0.1",
  "description": "Google Drive API to move files from one user to another",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bun run src/index.ts",
    "build": "bun build --target=node ./src/index.ts --outdir dist --splitting --sourcemap=inline"
  },
  "dependencies": {
    "arg": "^5.0.2",
    "googleapis": "^144.0.0",
    "opn": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@types/node": "^22.13.5"
  }
}
